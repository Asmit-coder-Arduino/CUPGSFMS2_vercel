{
  "buildCommand": "pnpm -w run build:vercel",
  "installCommand": "echo 'Dependencies handled in build script'",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
